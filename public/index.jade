extends layout

block page_script
  script
    $(document).ready(function(){
      $("#submit").click(function(){

        var $this = $( this );
        jQuery.post("/create", {
          "thankyou": $("#thanks").val()
        }, function(){
          $this.addClass( "btn-success" ).val( "Saved :)" );
        }).fail(function(){
          $this.addClass( "btn-danger" ).val( "Failed D:" );
        });

      });
    });

block content
  h1 Kiitollisuuspurkki
  textarea#thanks
  input(type="submit")#submit.btn.btn-large.btn-primary
